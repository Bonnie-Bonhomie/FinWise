import 'package:flutter/material.dart';

class UserAccountDetails extends StatelessWidget {
  const UserAccountDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("UserDetails Page"),
      ),
    );
  }
}
